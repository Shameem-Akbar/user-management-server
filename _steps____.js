/**
 * 1. create a post api on the server side
 * 2. send data via post from client side
 * 3. set fetch method inside the fetch options (2nd parameter)
 * 4. options will have three things: method
 * 4. options will have headers: "content-type": "application/json"
 * 5. body: don't forget to send data via json stringify 
 * 6. on the server side don't forget to use express.json() middleware
 */